% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize_markers.R
\name{harmonizeMarkers}
\alias{harmonizeMarkers}
\title{Harmonize cellular marker labels}
\usage{
harmonizeMarkers(markers)
}
\arguments{
\item{markers}{A character vector of marker labels to harmonize
(e.g. \code{c("CD3-FITC", "IL-5", "HLA-DR")}).}
}
\value{
A character vector of the same length as \code{markers}. Each element is
the standardised symbol, or \code{""} when no match was found in any lookup.
}
\description{
Maps raw marker labels (e.g. from FCS files) to standardised symbols using
a three-step lookup cascade:
}
\details{
\enumerate{
\item \strong{HGNC CD column} (\link{HGNC}): exact match against CD annotations from the
Human Cell Differentiation Molecule (HCDM) database. When no exact match
is found, a CD pattern (e.g. \code{"CD8"} from \code{"CD8-APC"}) is extracted and
looked up instead. Markers containing \code{";"} are skipped in this step as
they likely represent double-stained channels.
\item \strong{HGNC gene symbol} (\link{HGNC}): case-insensitive match against approved
HGNC gene symbols. If the initial match fails, special characters are
stripped and the lookup is retried (e.g. \code{"IL-5"} → \code{"IL5"}). \code{"B2M"} is
handled as a special case (common barcoding marker). When a match is
found, the CD annotation is returned if one exists; otherwise the HGNC
symbol is returned.
\item \strong{LUNC department lookup} (\link{LUNC}): case-insensitive match against the
internal FCS parameter description table used at LUMC.
}

If no match is found in any step, an empty string (\code{""}) is returned for
that element and a message is printed to the console.
}
\examples{
\dontrun{
harmonizeMarkers(c("CD3-FITC", "IL-5", "HLA-DR", "FSC-A"))
}

}
\seealso{
\link{HGNC}, \link{LUNC}, \code{\link[=harmonizeGates]{harmonizeGates()}}
}
\keyword{internal}
