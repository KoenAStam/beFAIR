% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_update.R
\name{databaseUpdate}
\alias{databaseUpdate}
\title{Update the FAIRground database}
\usage{
databaseUpdate(enable_logging = FALSE, con = NULL)
}
\arguments{
\item{enable_logging}{Logical. If \code{TRUE}, JSON logs are written to
\verb{logs/files/<timestamp>.json} using the \code{lgr} package. Defaults to \code{FALSE}.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Called for its side effects. Invisibly returns \code{NULL}.
}
\description{
The main orchestrator for a full database update cycle. Sequentially:
\enumerate{
\item Synchronizes codebook definitions with the database via
\code{\link[=databasePrepareCodebooks]{databasePrepareCodebooks()}}.
\item Discovers new, changed, or outdated ETL scripts via
\code{\link[=databasePrepareScripts]{databasePrepareScripts()}}.
\item For each script that requires (re)running: clears its existing database
entries, sources the script, validates the resulting \code{databook}, and
uploads the data via \code{\link[=databasePrepareData]{databasePrepareData()}}.
\item Rebuilds the overview tables (\code{study}, \code{experiment}, \code{analysis}) and the
\code{publications} table unconditionally.
}
}
\details{
The connection is also closed on exit.
}
\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
databaseUpdate()
databaseUpdate(enable_logging = TRUE)
}
}
\seealso{
\code{\link[=databaseInitialize]{databaseInitialize()}}, \code{\link[=databasePrepareCodebooks]{databasePrepareCodebooks()}},
\code{\link[=databasePrepareScripts]{databasePrepareScripts()}}, \code{\link[=databasePrepareData]{databasePrepareData()}},
\code{\link[=databaseUpdateOverview]{databaseUpdateOverview()}}, \code{\link[=databaseUpdatePublications]{databaseUpdatePublications()}}
}
