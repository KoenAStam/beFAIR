% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_tables_by_id.R
\name{collectTablesById}
\alias{collectTablesById}
\title{Collect all database tables linked to a study, experiment, or analysis ID}
\usage{
collectTablesById(id, recursive = TRUE, namesOnly = FALSE, con = NULL)
}
\arguments{
\item{id}{A character string specifying the ID to search for. The type
(\code{study_id}, \code{experiment_id}, or \code{analysis_id}) is inferred from the
format of the value.}

\item{recursive}{Logical. If \code{TRUE} (default), the function follows
relational links upward: analysis → linked experiments → linked studies.
If \code{FALSE}, only tables directly containing the supplied ID column are
queried.}

\item{namesOnly}{Logical. If \code{TRUE}, returns only a character vector of
table names that contain at least one row matching the ID, without
fetching the actual data. Default is \code{FALSE}.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
When \code{namesOnly = FALSE} (default): a named list of data frames,
one per matching table, with each data frame filtered to rows where the
ID column equals \code{id}. Empty tables are excluded. When \code{namesOnly = TRUE}:
a character vector of table names that contain matching rows.
}
\description{
Queries the database for all tables that contain a column matching the type
of the supplied ID (\code{study_id}, \code{experiment_id}, or \code{analysis_id}). The ID
type is inferred automatically from the ID format. When \code{recursive = TRUE},
the function chains upward through the data hierarchy: an \code{analysis_id}
triggers a follow-up lookup of its linked experiments, which in turn trigger
a lookup of their linked studies.
}
\details{
The ID type is determined by pattern matching:
\itemize{
\item IDs matching \verb{^A[0-9a-f]\{6\}$} are treated as \code{analysis_id}
\item IDs matching \verb{^E[0-9a-f]\{6\}$} are treated as \code{experiment_id}
\item All other IDs are treated as \code{study_id}
}

Table discovery uses \code{INFORMATION_SCHEMA.COLUMNS}, so this function
requires a SQL Server (ODBC) connection and is not compatible with SQLite.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")

# Retrieve all data linked to a study
collectTablesById(id = "HYPOV")

# Only return table names (no data fetched)
collectTablesById(id = "Ec0a1f3", namesOnly = TRUE)

# Retrieve analysis data without chaining to experiments/studies
collectTablesById(id = "A1b6b7f", recursive = FALSE)
}
}
\seealso{
\code{\link[=getDatabase]{getDatabase()}}, \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}, \code{\link[=searchId]{searchId()}}
}
