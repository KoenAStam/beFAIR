% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook_to_entries.R
\name{codebookToEntries}
\alias{codebookToEntries}
\title{Create an empty databook from a codebook}
\usage{
codebookToEntries(codebook_name, nrow = 0)
}
\arguments{
\item{codebook_name}{A character string specifying the codebook name (without
\code{.yaml} extension), e.g. \code{"participant"} or \code{"cytofpanel"}.}

\item{nrow}{An integer specifying the number of rows to initialise in the
data frame. Defaults to \code{0}.}
}
\value{
A named list with the following elements:
\itemize{
\item \code{codebook}: The variables map read from the codebook YAML.
\item \code{codebook_name}: The codebook name as supplied in \code{codebook_name}.
\item \code{codebook_version}: The codebook version string (e.g. \code{"2.1"}).
\item \code{data}: A data frame with columns matching the codebook variables
and R types derived from the codebook \code{type} field (\code{character},
\code{numeric}, \code{integer}, or \code{Date}).
\item \code{validation}: A list with \code{status = 0} and \code{summary = NULL},
to be populated by \code{\link[=validateDatabook]{validateDatabook()}}.
}
}
\description{
Reads the named codebook YAML, creates an empty data frame with the correct
R column types, and returns both bundled into a \code{databook} object. The
\code{databook} is the standard container that ETL scripts populate and that
\code{\link[=databasePrepareData]{databasePrepareData()}} consumes.
}
\examples{
\dontrun{
databook <- codebookToEntries("participant", nrow = 50)

codebookToSummary("cytofpanel")
databook <- codebookToEntries("cytofpanel")
}
}
\seealso{
\code{\link[=codebookRead]{codebookRead()}}, \code{\link[=validateDatabook]{validateDatabook()}},
\code{\link[=databasePrepareData]{databasePrepareData()}}, \code{\link[=codebookToSummary]{codebookToSummary()}}, \code{\link[=addStudy]{addStudy()}},
\code{\link[=addExperiment]{addExperiment()}}, \code{\link[=addAnalysis]{addAnalysis()}}
}
