% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_export.R
\name{databaseExport}
\alias{databaseExport}
\title{Export all database tables to R}
\usage{
databaseExport(
  path = NULL,
  overwrite = FALSE,
  stripped = FALSE,
  replace_missing = TRUE,
  study_ids = NULL,
  remove_id_mapping = TRUE
)
}
\arguments{
\item{path}{Character or NULL. Directory where the .rds file should be saved.
If \code{NULL}, no file is written and the list is returned directly.}

\item{overwrite}{Logical. If \code{TRUE}, an existing .rds file at \code{path} is
overwritten. If \code{FALSE} (default) and the file exists, saving is skipped with
a warning.}

\item{stripped}{Logical. If \code{TRUE}, only non-sensitive tables are returned
(study-, experiment-, and analysis-level tables, panel tables, event, and
publications). Default: \code{FALSE}.}

\item{replace_missing}{Logical. If \code{TRUE}, values \code{-999}, \code{"N/A"},
and \code{"N/M"} are replaced with \code{NA}. Default: \code{TRUE}.}

\item{study_ids}{Character vector or NULL. If provided, only rows linked to
these study IDs are retained via \code{\link[=filterTableList]{filterTableList()}}.}
}
\value{
A named list of data frames containing the database tables. If \code{path}
is provided, the list is also saved as an .rds file in that directory. The
filename depends on the parameters used:
\itemize{
\item \code{table_list.rds} — default
\item \code{table_list_stripped.rds} — when \code{stripped = TRUE}
\item \code{table_list_filtered.rds} — when \code{study_ids} is provided
}
}
\description{
Fetches all tables from the beFAIR SQL database and optionally saves the
result as an .rds file. Unlike \code{getDatabase()}, this function always
queries the database directly — it never reads from a local cache.
}
\examples{
\dontrun{
  # Fetch from DB and return without saving
  data <- databaseExport()

  # Fetch from DB and save to disk
  data <- databaseExport(path = "data/")

  # Force overwrite an existing cache file
  data <- databaseExport(path = "data/", overwrite = TRUE)

  # Fetch stripped dataset for selected studies
  data <- databaseExport(path = "data/", stripped = TRUE, study_ids = c("CHHIL", "ITCHI"))
}
}
\seealso{
\code{\link[=getDatabase]{getDatabase()}} for a cache-first wrapper around this function.
}
