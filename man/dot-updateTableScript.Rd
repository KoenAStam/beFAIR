% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-db.R
\name{.updateTableScript}
\alias{.updateTableScript}
\title{Update a script record in \code{table_script}}
\usage{
.updateTableScript(con = NULL, script_id, databook)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}

\item{script_id}{A character string identifying the ETL script. Used both as
the database key and the file path from which the MD5 hash is computed.}

\item{databook}{A named list as produced by \code{\link[=sourceScript]{sourceScript()}}. Must contain:
\itemize{
\item \code{codebook_name}: Character scalar; target table name in the database.
\item \code{codebook_version}: Character scalar; version identifier for the codebook.
\item \code{run_time}: Numeric scalar; duration of the ETL script execution in seconds.
\item \code{flag_TODO}: Logical scalar; whether the script contains TODO flags.
\item \code{flag_WARN}: Logical scalar; whether the script produced warnings.
}}
}
\value{
Invisibly returns \code{TRUE} on success or \code{FALSE} if the update query fails.
}
\description{
Updates the \code{table_script} row identified by \code{script_id} with the MD5 hash
of the current script file, codebook metadata, run time, and any warning or
TODO flags from the most recent ETL run. Called internally by \code{\link[=databaseUpdate]{databaseUpdate()}}
after each script is sourced and validated.
}
\seealso{
\code{\link[=databaseUpdate]{databaseUpdate()}}, \code{\link[=sourceScript]{sourceScript()}}
}
\keyword{internal}
