% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_script.R
\name{sourceScript}
\alias{sourceScript}
\title{Source an ETL script and return its databook}
\usage{
sourceScript(script_id, echo = FALSE, con = NULL)
}
\arguments{
\item{script_id}{Character. Relative path to the ETL script (used as the
script identifier).}

\item{echo}{Logical. Passed to \code{\link[base:source]{base::source()}}; whether each expression is
printed as it is evaluated. Default: \code{FALSE}.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
A \code{databook} list containing \verb{$data}, \verb{$codebook},
\verb{$codebook_name}, \verb{$codebook_version}, \verb{$validation}, \verb{$run_time},
\verb{$flag_TODO}, and \verb{$flag_WARN}. Returns \code{NULL} if the script fails.
}
\description{
Executes an ETL script in an isolated environment, captures warnings and
counts \verb{#TODO} flags, and returns the \code{databook} object produced by the
script. Returns \code{NULL} if the script errors.
}
\examples{
\dontrun{
  databook <- sourceScript("studies/CHHIL/scripts/participant.R")
}

}
\seealso{
\code{\link[=databaseUpdate]{databaseUpdate()}}, \code{\link[=databasePrepareScripts]{databasePrepareScripts()}}
}
