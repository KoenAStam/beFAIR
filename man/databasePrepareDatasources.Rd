% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_prepare_datasources.R
\name{databasePrepareDatasources}
\alias{databasePrepareDatasources}
\title{Prepare data source registry}
\usage{
databasePrepareDatasources(con = NULL)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly \code{NULL}. Called for its side effects.
}
\description{
Scans the FAIRground directories for data-source YAML files (files
located in a \verb{data/} subfolder under \verb{studies/}, \verb{experiments/}, and
\verb{analyses/}), reads their metadata, computes MD5 hashes for the actual data
files they reference, and synchronises \code{table_datasource} in the database.
}
\details{
The function identifies and tracks:
\itemize{
\item \strong{New YAML files} not yet present in \code{table_datasource} — inserted with
\code{present_in_project = 1}.
\item \strong{Existing YAML files} — all metadata fields and \code{file_hash} are refreshed.
\item \strong{Gone YAML files} whose path is no longer found on disk — marked with
\code{present_in_project = 0}; never deleted from the registry.
}
}
\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
databasePrepareDatasources()
}

}
\seealso{
\code{\link[=databaseUpdate]{databaseUpdate()}}, \code{\link[=databasePrepareScripts]{databasePrepareScripts()}}
}
