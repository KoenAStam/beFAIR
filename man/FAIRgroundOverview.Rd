% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overview_fairground.R
\name{FAIRgroundOverview}
\alias{FAIRgroundOverview}
\alias{overviewFAIRground}
\title{Generate an overview of beFAIR meta.yaml files}
\usage{
FAIRgroundOverview(print = TRUE, print_full = FALSE)

overviewFAIRground(...)
}
\arguments{
\item{print}{Logical. If TRUE (default), prints a summary preview to the console.}

\item{print_full}{Logical. If TRUE, prints all rows (up to 5 columns). Default is FALSE.}
}
\value{
A named list of data frames. The list always contains three entries
corresponding to the main pillars: \code{studies}, \code{experiments}, and \code{analyses}.
When extended codebooks exist (e.g. \code{experiment_cytof}), additional entries
named after those codebooks are included, each filtered to the matching \code{type}.
Data frames contain only the columns defined in the corresponding codebook;
missing columns are silently dropped. An empty data frame is returned for
pillars that have no \code{meta.yaml} files.
}
\description{
This function reads and combines all \code{meta.yaml} files located under the
\code{studies}, \code{experiments}, and \code{analyses} directories. It returns a named
list containing data frames for each pillar, with each data frame containing
the standardized columns according to the respective codebooks.
}
\details{
The function prints a preview of the main overviews. By default,
only the first 5 rows and first 5 columns are shown. Set \code{print_full = TRUE}
to display all rows (still limited to 5 columns for readability).
}
\examples{
FAIRgroundOverview()
overview <- FAIRgroundOverview()
}
