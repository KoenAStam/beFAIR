% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{FAIRgroundConnection}
\alias{FAIRgroundConnection}
\title{Get the active FAIRground database connection}
\usage{
FAIRgroundConnection(con = NULL)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used. If supplied, it must be a valid, open
\code{DBIConnection} object.}
}
\value{
A valid, open \code{DBIConnection} object. Throws an error if no valid
connection is available.
}
\description{
Returns the provided connection if valid, or falls back to the package
default connection stored by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}. Used internally by most
beFAIR functions to resolve a \code{con = NULL} argument.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")

## Resolve the default connection
con <- FAIRgroundConnection()

## Pass an explicit connection (validated and returned as-is)
con <- FAIRgroundConnection(con = my_con)
}
}
\seealso{
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}, \code{\link[=FAIRgroundDisconnect]{FAIRgroundDisconnect()}}
}
