% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_prepare_projects.R
\name{databasePrepareProjects}
\alias{databasePrepareProjects}
\title{Prepare the pillar project registry}
\usage{
databasePrepareProjects(con = NULL)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly \code{NULL}. Called for its side effects.
}
\description{
Scans the FAIRground directories for pillar \code{meta.yaml} files
(\verb{studies/}, \verb{experiments/}, \verb{analyses/}), computes folder hashes for
their \verb{scripts/} subdirectory, and synchronises \code{table_project} in the
database.
}
\details{
For analyses the function also:
\enumerate{
\item Parses the optional \code{dependencies} field in \code{meta.yaml}.
\item Upserts \code{analysis_input} rows into \code{table_dependency}, resolving each
declared dependency to the corresponding \code{script_id} in
\code{table_script}.
\item Flags analyses as \code{up_to_date = 0} when any resolved dependency
script has \code{queued_for_update = 1} in \code{table_script}.
}
}
\section{Dependency syntax}{

The \code{dependencies} field in an analysis \code{meta.yaml} is a YAML list where
each entry has the form \verb{<codebook_name>[<id1>,<id2>,...]}.  \code{id} values
are the short pillar IDs stored in \code{table_project.project_id} (e.g. study
\code{"CHSI1"} or experiment \code{"Eef536a"}).  Multiple IDs for the same codebook
are comma-separated inside the brackets.

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{dependencies:
  - participant[CHSI1]
  - cytofpanel[Eef536a,Eaa52ef]
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
databasePrepareProjects()
}

}
\seealso{
\code{\link[=databaseUpdate]{databaseUpdate()}}, \code{\link[=databasePrepareScripts]{databasePrepareScripts()}}
}
