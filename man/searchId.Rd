% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_id.R
\name{searchId}
\alias{searchId}
\title{Search for studies, experiments, or analyses by ID or name}
\usage{
searchId(query, con = NULL)
}
\arguments{
\item{query}{A character string to search for. Partial matches are supported
(e.g. \code{"hypo"} will match \code{"HypoVax"} or \code{"HYPOV"}).}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{pillar}: one of \code{"study"}, \code{"experiment"}, or \code{"analysis"}
\item \code{id}: the ID value (\code{study_id}, \code{experiment_id}, or \code{analysis_id})
\item \code{name}: the human-readable name
}

Returns an empty data frame with the same columns when no matches are found.
}
\description{
Performs a case-insensitive partial-match search against the \code{study},
\code{experiment}, and \code{analysis} tables in the FAIRground database. The query
is matched against both the ID column and the \code{name} column of each table.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")
searchId("NIH")
searchId("A")
searchId("ELISA")
}
}
\seealso{
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}, \code{\link[=FAIRgroundOverview]{FAIRgroundOverview()}}
}
