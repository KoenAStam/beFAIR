% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_codebook_map.R
\name{viewCodebookMap}
\alias{viewCodebookMap}
\title{View Codebook Relationship Map (ERD Style)}
\usage{
viewCodebookMap(id, output = NULL, con = NULL)
}
\arguments{
\item{id}{Character. A beFAIR ID to look up.}

\item{output}{Character or \code{NULL}. If \code{NULL} (default), the diagram is
rendered in the RStudio Viewer. Supply a \code{.html} file path to save a
self-contained HTML file instead.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly returns the \code{grViz} htmlwidget when saving; returns the
widget visibly (auto-displays in Viewer) otherwise.
}
\description{
Calls \code{\link[=collectTablesById]{collectTablesById()}} to determine which tables are relevant for a
given ID, then renders an ERD-style diagram where each codebook appears as a
SQL table node listing its primary keys and foreign keys.
}
\details{
The \code{id} can be any beFAIR identifier:
\itemize{
\item Analysis ID (e.g. \code{"Ae0f301"}) — traces through experiments to studies
\item Experiment ID (e.g. \code{"E4fe387"}) — traces through to studies
\item Study ID (e.g. \code{"CHSI1"}) — shows study-level tables only
}
}
\examples{
\dontrun{
  FAIRgroundConnect(dsn = "LUCID_FAIRPOINT")
  viewCodebookMap("Adf961e")
  viewCodebookMap("E133618", output = "codebook_map.html")
}
}
\seealso{
\code{\link[=collectTablesById]{collectTablesById()}}, \code{\link[=searchId]{searchId()}}
}
