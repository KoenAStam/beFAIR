% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_prepare_scripts.R
\name{databasePrepareScripts}
\alias{databasePrepareScripts}
\title{Prepare ETL scripts prior to updating the database}
\usage{
databasePrepareScripts(con = NULL)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
A data frame with one row per ETL script queued for (re)running
(\code{queued_for_update == 1 & suspended == 0}), with all \code{table_script} columns.
Rows are ordered by category (\code{studies}, \code{experiments}, \code{analyses}) and a
predefined study dependency order.
}
\description{
Scans the FAIRground directories for ETL scripts (files located in a
\verb{scripts/} subfolder under \verb{studies/}, \verb{experiments/}, and \verb{analyses/}),
computes an MD5 hash for each script, and maintains \code{table_script} as the
authoritative status table.
}
\details{
The function identifies and tracks:
\itemize{
\item \strong{New scripts} not yet present in \code{table_script} — inserted with \code{queued_for_update = 1}.
\item \strong{Changed scripts} whose content hash differs from the database — \code{queued_for_update} set to 1.
\item \strong{Outdated scripts} whose \code{codebook_version} lags behind \code{table_codebook} — \code{queued_for_update} set to 1.
\item \strong{Suspended scripts} whose filename starts with \verb{_} — recorded in \code{table_script} with \code{suspended = 1} and skipped.
}

\strong{Database effects:} This function may INSERT or UPDATE rows in \code{table_script}.
New internal scripts are only inserted when the inferred \code{codebook_name} exists
in \code{table_codebook}; otherwise the script is skipped and an error is logged.

\strong{Suspended scripts:} ETL scripts with a leading underscore in the filename are
treated as suspended. Their \code{live_id} (path with \verb{_} removed) is recorded in
\code{table_script} with \code{suspended = 1} and \code{queued_for_update = 0}.
}
\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
scriptsToUpdate <- databasePrepareScripts()
}

}
\seealso{
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}, \code{\link[=databaseUpdate]{databaseUpdate()}}, \code{\link[=databasePrepareCodebooks]{databasePrepareCodebooks()}}
}
