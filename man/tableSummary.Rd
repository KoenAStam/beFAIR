% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_summary.R
\name{tableSummary}
\alias{tableSummary}
\title{Summarise all variables in a codebook as a \code{gt} table}
\usage{
tableSummary(data, codebook_name)
}
\arguments{
\item{data}{A data frame containing the data to summarise, structured
according to \code{codebook_name}.}

\item{codebook_name}{A character string specifying the codebook name
(without \code{.yaml} extension).}
}
\value{
A \code{gt} table object with one row per codebook variable. The table
header shows the codebook name and total row count. Completeness is
colour-coded on a red-yellow-green scale.
}
\description{
Produces a \code{gt} summary table for all variables defined in the named
codebook. For each variable, the table reports its type, completeness,
percentage not applicable (\code{-999} / \code{"N/A"}), percentage not measured
(\code{"N/M"}), mean, SD, and median, min, max (numeric variables), or the
set of unique values (character variables).
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")
data <- DBI::dbReadTable(FAIRgroundConnection(), "participant")
tableSummary(data, "participant")
}
}
\seealso{
\code{\link[=tableVariableByStudy]{tableVariableByStudy()}}, \code{\link[=codebookRead]{codebookRead()}},
\code{\link[=codebookToSummary]{codebookToSummary()}}
}
