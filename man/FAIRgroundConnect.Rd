% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{FAIRgroundConnect}
\alias{FAIRgroundConnect}
\title{Connect to a FAIRground database}
\usage{
FAIRgroundConnect(
  dsn = NULL,
  drv = NULL,
  driver = NULL,
  server = NULL,
  database = NULL,
  uid = NULL,
  pwd = NULL,
  trusted = TRUE,
  ...
)
}
\arguments{
\item{dsn}{Character scalar. For ODBC, the ODBC Data Source Name (DSN).
For SQLite (when \code{drv = RSQLite::SQLite()}), this is interpreted as the
SQLite \code{dbname}, such as \code{":memory:"} or a file path.}

\item{drv}{A DBI driver object, e.g. \code{odbc::odbc()} or \code{RSQLite::SQLite()}.
If \code{NULL}, defaults to \code{odbc::odbc()}.}

\item{driver, server, database, uid, pwd}{Optional fields for DSN-less ODBC
connections. Ignored when \code{dsn} is supplied for ODBC.
For SQLite, \code{database} can be used as the SQLite \code{dbname}
when \code{dsn} is not provided.}

\item{trusted}{Logical. For DSN-less ODBC connections, whether to use
Windows integrated authentication (\code{Trusted_Connection = "Yes"}).
If \code{FALSE}, \code{uid} and \code{pwd} are used instead.}

\item{...}{Additional arguments passed to
\code{\link[DBI:dbConnect]{DBI::dbConnect()}}. Useful for driver-specific options such as
\code{Port}, \code{Timeout}, etc.}
}
\value{
Invisibly returns a \code{DBIConnection} object and stores it
as the package default connection in \code{.pkgenv$con}.
}
\description{
Creates (and stores) the package's \emph{default} DBI connection in \code{.pkgenv$con}.
If a previous default connection exists and is still valid, it is disconnected
before opening a new one.
}
\details{
The function supports two connection styles:
\itemize{
\item \strong{ODBC}: Use either a DSN (\code{dsn}) or DSN-less connection fields
(\code{driver}, \code{server}, \code{database}, \code{uid}, \code{pwd}, \code{trusted}).
\item \strong{SQLite}: When \code{drv = RSQLite::SQLite()}, \code{dsn} (preferred) or
\code{database} is treated as the SQLite \code{dbname} (e.g. \code{":memory:"}
or a file path).
}
}
\examples{
\dontrun{
## ODBC via DSN
FAIRgroundConnect(dsn = "LUCID_FAIRPOINT")

## the connection is also invisibly returned
con <- FAIRgroundConnect(dsn = "LUCID_FAIRPOINT")

## ODBC without DSN (DSN-less)
FAIRgroundConnect(
  driver   = "{ODBC Driver 18 for SQL Server}",
  server   = "localhost",
  database = "beFAIR",
  trusted  = TRUE
)

## SQLite
FAIRgroundConnect(drv = RSQLite::SQLite(), dsn = ":memory:") # in memory
FAIRgroundConnect(drv = RSQLite::SQLite(), dsn = "fairpoint.sqlite") # file-based

}

}
