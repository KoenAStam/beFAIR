% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dependencies.R
\name{plotDependencies}
\alias{plotDependencies}
\title{Plot the dependency graph for a table produced by an analysis}
\usage{
plotDependencies(table_name, id, con = NULL)
}
\arguments{
\item{table_name}{Character. The codebook/table name produced by the
analysis (e.g. \code{"som_clusterphenotype"}). Used as the root node label.}

\item{id}{Character. The \code{project_id} of the analysis (the 6-character
hex \code{analysis_id} from \code{meta.yaml}).}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly returns the \code{igraph} graph object. Called primarily for
its side effect of drawing a plot.
}
\description{
Queries \code{table_dependency} and draws a three-level directed graph:
\enumerate{
\item The analysis node (root), labelled with \code{table_name} and \code{id}.
\item Its \code{analysis_input} upstream scripts (the ETL scripts in other
pillars that produce the data the analysis declared as dependencies).
\item Each upstream script's own \code{datasource} and \code{script} dependencies
(data files and id-mapping scripts).
}
}
\details{
Node colours: blue = analysis, orange = script, green = datasource,
red = unresolved dependency (registered in meta.yaml but not yet in
\code{table_script}).

Requires the \pkg{igraph} package.
}
\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
plotDependencies("som_clusterphenotype", "ab12cd")
}

}
\seealso{
\code{\link[=databasePrepareProjects]{databasePrepareProjects()}}, \code{\link[=databasePrepareDependencies]{databasePrepareDependencies()}}
}
