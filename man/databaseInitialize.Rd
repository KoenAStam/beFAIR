% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_initialize.R
\name{databaseInitialize}
\alias{databaseInitialize}
\title{Initialize FAIRground database}
\usage{
databaseInitialize(
  overwrite_reference = TRUE,
  overwrite_core = FALSE,
  drop_all = FALSE,
  con = NULL
)
}
\arguments{
\item{overwrite_reference}{Logical. If \code{TRUE}, existing reference tables
(\code{HGNC}, \code{LUNC}) are dropped, recreated, and repopulated with the
built-in data. Default is \code{TRUE}.}

\item{overwrite_core}{Logical. If \code{TRUE}, the core registry tables
(\code{table_script}, \code{table_codebook}) are dropped and recreated. \strong{All
existing run history will be lost.} Default is \code{FALSE}.}

\item{drop_all}{Logical. If \code{TRUE}, all beFAIR-managed tables are dropped
before initialization proceeds. Only tables known to beFAIR (core, reference,
and codebook-derived data tables) are removed. Default is \code{FALSE}.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by
\code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Creates the core beFAIR tables in the connected database.
Specifically, this function generates the following tables based on
their registered codebooks:
}
\details{
\itemize{
\item \code{table_script}
\item \code{table_codebook}
\item \code{table_datasource}
\item \code{table_dependency}
\item \code{table_project}
}

It also creates and populates built-in reference tables:
\itemize{
\item \code{HGNC} — HUGO Gene Nomenclature Committee gene symbols and CD annotations
\item \code{LUNC} — LUCID internal nomenclature mapping FCS parameter descriptions to standard symbols
}

Table definitions are derived from the internal codebook registry and
created using \code{.sqlGenerateTable()}.

\strong{Reference tables} (\code{HGNC}, \code{LUNC}) are expected to be updated regularly
as nomenclature evolves. Set \code{overwrite_reference = TRUE} to drop and
recreate them and re-insert the built-in data.

\strong{Core registry tables} (\code{table_script}, \code{table_codebook}, \code{table_dependency},
\code{table_project}) track ETL history and should only be recreated in exceptional
circumstances (e.g. a major schema upgrade). Set \code{overwrite_core = TRUE} to
do so; this will \strong{destroy all existing run history}. Use with caution.

\strong{Full reset} (\code{drop_all = TRUE}) drops all beFAIR-managed tables before
creating the core tables. Only tables known to beFAIR are affected — core
registry tables, reference tables, and any data table registered in a
codebook (built-in or custom). Other tables in the database (e.g. from other
applications) are left untouched. Implies \code{overwrite_core = TRUE} and
\code{overwrite_reference = TRUE}.
}
\examples{
\dontrun{
library(beFAIR)
FAIRgroundConnect(drv = RSQLite::SQLite(), dsn = ":memory:")

# First-time setup
databaseInitialize()

# Refresh reference tables with updated nomenclature
databaseInitialize(overwrite_reference = TRUE)

# Schema upgrade (destroys run history)
databaseInitialize(overwrite_core = TRUE)

# Full reset: wipe everything and start from scratch
databaseInitialize(drop_all = TRUE)
}

}
