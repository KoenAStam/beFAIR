% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_variable_by_study.R
\name{tableVariableByStudy}
\alias{tableVariableByStudy}
\title{Summarise a codebook variable by study as a \code{gt} table}
\usage{
tableVariableByStudy(var, data, codebook_name)
}
\arguments{
\item{var}{A character string specifying the variable to summarise. Must be
defined in \code{codebook_name}.}

\item{data}{A data frame containing the data to summarise. Must include a
\code{study_id} column and the variable specified by \code{var}.}

\item{codebook_name}{A character string specifying the codebook name
(without \code{.yaml} extension).}
}
\value{
A \code{gt} table object with rows per \code{study_id}. Completeness is
colour-coded on a red-yellow-green scale. Returns a placeholder \code{gt} table
for unsupported variable types.
}
\description{
Reads the named codebook to determine the variable type and produces a
colour-coded \code{gt} table summarising the variable grouped by \code{study_id}.
For \code{character} variables, the set of unique values and completeness are
reported. For \code{numeric} variables, mean, SD, median, min, max and
completeness are shown.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")
data <- DBI::dbReadTable(FAIRgroundConnection(), "participant")
tableVariableByStudy("sex", data, "participant")
}

}
\seealso{
\code{\link[=codebookToSummary]{codebookToSummary()}}, \code{\link[=codebookRead]{codebookRead()}}, \code{\link[=tableSummary]{tableSummary()}}
}
