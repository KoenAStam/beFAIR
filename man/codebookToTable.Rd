% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook_to_table.R
\name{codebookToTable}
\alias{codebookToTable}
\title{Compile all codebooks into a data frame}
\usage{
codebookToTable()
}
\value{
A data frame with one row per codebook and the following columns:
\itemize{
\item \code{codebook_id}: Relative path identifier (e.g. \code{"study/participant"}).
\item \code{codebook_name}: The \code{name} field from the codebook YAML.
\item \code{codebook_version}: The version string (e.g. \code{"2.1"}).
\item \code{authors}: Comma-separated list of authors, or \code{"Unknown"} if absent.
\item \code{created_at}: Creation date from the YAML metadata.
\item \code{updated_at}: Last update date from the YAML metadata, or \code{NA}.
\item \code{n_parameters}: Number of variables defined in the codebook.
\item \code{codebook_origin}: \code{"beFAIR"} for built-in codebooks; the project
directory basename for custom codebooks.
}
}
\description{
Scans the \verb{study/}, \verb{experiment/}, and \verb{analyses/} codebook directories
bundled with the package, reads each YAML file, and returns the metadata
as a data frame. If a \verb{codebooks/} folder exists in the current working
directory, custom codebooks found there are included as well.
}
\details{
Used internally by \code{\link[=databasePrepareCodebooks]{databasePrepareCodebooks()}} to populate \code{table_codebook}.
}
\examples{
\dontrun{
codebookToTable()
}
}
\seealso{
\code{\link[=databasePrepareCodebooks]{databasePrepareCodebooks()}}, \code{\link[=codebookRead]{codebookRead()}},
\code{\link[=codebookToSummary]{codebookToSummary()}}
}
