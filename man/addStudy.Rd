% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_study.R
\name{addStudy}
\alias{addStudy}
\title{Add a study to the FAIRground}
\usage{
addStudy(study_name, study_id, registration_code = NULL)
}
\arguments{
\item{study_name}{A character string specifying the name of the study.
Used as the directory name under \verb{studies/}.}

\item{study_id}{A unique identifier for the study. Must be exactly 5
alphanumeric characters. Used as a prefix when generating
\code{participant_id} values.}

\item{registration_code}{Optional. The ClinicalTrials.gov registration
code (e.g. "NCT04269915"). If provided, additional study metadata
is fetched from ClinicalTrials.gov and merged into \code{meta.yaml}.}
}
\value{
Called for its side effects. Creates the study directory and
subdirectories (\verb{scripts/}, \verb{data/}), writes \code{meta.yaml}, copies a
data template, and drafts four ETL scripts. Prints a message on success.
}
\description{
Creates a study directory structure under \verb{studies/}, writes a \code{meta.yaml},
copies a data template, and drafts the standard ETL scripts for the
\code{id_mapping}, \code{participant}, \code{studyarm}, and \code{studydesign} codebooks.
If a \code{registration_code} is provided, study metadata is fetched from
ClinicalTrials.gov and merged into the \code{meta.yaml}.
}
\examples{
\dontrun{
addStudy("CoHSI1", "CHSI1", "NCT04269915")
addStudy("MyStudy", "MSTUDY")
}
}
\seealso{
\code{\link[=addExperiment]{addExperiment()}}, \code{\link[=addAnalysis]{addAnalysis()}}, \code{\link[=draftScript]{draftScript()}}
}
