% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-db.R
\name{.hash}
\alias{.hash}
\title{Hash a file, directory, or string}
\usage{
.hash(x, type, algo = "md5", serialize = TRUE)
}
\arguments{
\item{x}{Path (for \code{type = "content"}) or an R object
(for \code{type = "string"}).}

\item{type}{\code{"content"} (file/directory) or \code{"string"}.}

\item{algo}{Hash algorithm forwarded to \code{\link[digest]{digest}}.
Defaults to \code{"md5"}.}

\item{serialize}{Forwarded to \code{\link[digest]{digest}}.}
}
\value{
A character scalar hash, or \code{NA_character_} if the path is
missing or the directory is empty.
}
\description{
Hash a file, directory, or string
}
\details{
\strong{File}: raw bytes are hashed via \code{digest(file = x)}.
\strong{Directory}: a metadata hash of each file's mtime and size (sorted
by path) is produced — detects additions, removals, renames, and
size/mtime changes, but not in-place edits that preserve both.
\strong{String}: \code{x} is passed directly to \code{digest()}.
}
\keyword{internal}
