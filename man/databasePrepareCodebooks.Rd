% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_prepare_codebooks.R
\name{databasePrepareCodebooks}
\alias{databasePrepareCodebooks}
\title{Prepare and synchronize beFAIR codebooks with the database}
\usage{
databasePrepareCodebooks(con = NULL)
}
\arguments{
\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects on the database.
}
\description{
Builds a codebook metadata overview from the beFAIR package and
synchronizes it with the database table \code{table_codebook}. New codebooks are
inserted and their corresponding SQL tables are created. For existing
codebooks, version changes are detected and handled as follows:
}
\details{
\itemize{
\item \strong{Major version increase}: drops and recreates the corresponding data
table, and replaces the metadata row in \code{table_codebook}.
\item \strong{Minor version increase}: updates metadata in \code{table_codebook}
(e.g., version, authors, update date, and parameter count).
}

\strong{Database requirements:} This function expects the metadata table
\code{table_codebook} to exist and be readable.

\strong{Database effects:}
\itemize{
\item Inserts rows into \code{table_codebook} for newly detected codebooks.
\item Creates SQL data tables for new codebooks via \code{.sqlGenerateTable()}.
\item On major updates, executes \verb{DROP TABLE IF EXISTS <codebook_name>} and
recreates the table, then replaces the corresponding \code{table_codebook} row.
\item On minor updates, updates \code{table_codebook} fields in-place.
}

\strong{Version semantics:} Codebook versions are interpreted as \code{major.minor}
numeric pairs. Only increases trigger database actions; equal versions result
in no changes.
}
\examples{
\dontrun{
FAIRgroundConnect("MY_DSN")
databasePrepareCodebooks()
}

}
\seealso{
\code{\link[=codebookToTable]{codebookToTable()}}, \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}}, \code{\link[=FAIRgroundConnection]{FAIRgroundConnection()}}
}
