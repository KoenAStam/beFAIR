% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_relative_timepoint.R
\name{getRelativeTimepoint}
\alias{getRelativeTimepoint}
\title{Convert timepoint descriptions to relative timepoints in days}
\usage{
getRelativeTimepoint(
  study_id,
  source_id,
  timepoint_description,
  verbose = FALSE,
  con = NULL
)
}
\arguments{
\item{study_id}{A character vector of study identifiers, one per row of input.}

\item{source_id}{A character vector of original participant identifiers
(source IDs), one per row of input.}

\item{timepoint_description}{A character vector of timepoint descriptions
as used in the study design (e.g. \code{"C1"}, \code{"I2+14"}, \code{"I_LAST+14"}, \code{"C-7"}).}

\item{verbose}{Logical. If \code{TRUE}, messages the study design timeline with
the resolved time points. Defaults to \code{FALSE}.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
A data frame with columns \code{study_id}, \code{source_id},
\code{timepoint_description}, and \code{timepoint} (integer, days relative to the
anchor day). Rows where the description could not be resolved have \code{NA}
in \code{timepoint}.
}
\description{
Resolves textual timepoint descriptions (e.g. \code{"C1"}, \code{"I2+14"}) to numeric
timepoints in days relative to the anchor day, by joining against the
\code{id_mapping}, \code{participant}, and \code{studydesign} database tables.
Timepoint descriptions must follow the pattern \verb{<C|I><number>[+/-offset]},
for example \code{"C1"}, \code{"C-7"}, \code{"I_LAST+14"}, or \code{"I3+14"}. Invalid descriptions produce a
warning and result in \code{NA} for the corresponding rows.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT")
getRelativeTimepoint(
  study_id             = c("CoHSI1", "PfGA1"),
  source_id            = c("CS1-229", "BMV_021"),
  timepoint_description = c("C-1", "I1+14")
)
}
}
\seealso{
\code{\link[=codebookToSummary]{codebookToSummary()}}
}
