% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-ids.R
\name{.addParticipantIds}
\alias{.addParticipantIds}
\title{Add a stable \code{participant_id} to an ID mapping table}
\usage{
.addParticipantIds(id_mapping)
}
\arguments{
\item{id_mapping}{A data frame with columns \code{source_id}, \code{study_id}, and
\code{participant_nr} (integer).}
}
\value{
A data.frame with columns \code{source_id}, \code{study_id}, \code{participant_nr},
and \code{participant_id}.
}
\description{
Creates a deterministic participant identifier derived from \code{study_id} and
\code{participant_nr}.
}
\details{
The identifier is constructed by concatenating \code{study_id} and a zero-padded
(4-digit) \code{participant_nr}, hashing the key with SHA-256, and taking the
first six hexadecimal characters. Final form: \code{"<study_id>_<hash6>"}.

Multiple \code{source_id} values may map to the same participant when they share
the same \code{study_id} and \code{participant_nr}. Hash truncation means collisions
are theoretically possible but very unlikely.
}
\keyword{internal}
