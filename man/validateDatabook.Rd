% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_data_by_codebook.R
\name{validateDatabook}
\alias{validateDatabook}
\title{Validate a databook against its codebook rules}
\usage{
validateDatabook(databook, print = TRUE, con = NULL)
}
\arguments{
\item{databook}{A databook list as produced by \code{\link[=codebookToEntries]{codebookToEntries()}},
containing \verb{$data}, \verb{$codebook}, and \verb{$codebook_name}.}

\item{print}{Logical. If \code{TRUE} (default), prints the pointblank agent
report to the console.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
A list with two elements:
\itemize{
\item \verb{$status}: \code{1} if all validations passed, \code{0} if any failed.
\item \verb{$summary}: the interrogated pointblank agent object.
}
}
\description{
Applies the validation rules defined in the codebook to the \code{databook$data}
data frame using \code{pointblank}. Also checks that all \code{source_id} values exist
in the \code{id_mapping} table (unless the codebook is \code{id_mapping} itself).
}
\examples{
\dontrun{
  databook <- codebookToEntries("participant", nrow = 0)
  result <- validateDatabook(databook, print = FALSE)
}

}
\seealso{
\code{\link[=codebookToEntries]{codebookToEntries()}}
}
