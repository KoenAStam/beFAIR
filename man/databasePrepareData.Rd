% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_prepare_data.R
\name{databasePrepareData}
\alias{databasePrepareData}
\title{Prepare data from a \code{databook} for uploading to database}
\usage{
databasePrepareData(databook, script_id, con = NULL)
}
\arguments{
\item{databook}{A list as generated by \code{\link[=sourceScript]{sourceScript()}}. Must contain at
least:
\itemize{
\item \verb{$data}: A data frame with the table to upload.
\item \verb{$codebook_name}: Character; target table name in the database.
\item \verb{$codebook_version}: Character; version identifier for the codebook.
}}

\item{script_id}{Character. Both the identifier and file path of the ETL
script; read to compute the MD5 hash.}

\item{con}{A DBI connection. If \code{NULL}, the default connection set by \code{\link[=FAIRgroundConnect]{FAIRgroundConnect()}} is used.}
}
\value{
\code{TRUE} if data was successfully pushed and \code{table_script} updated,
\code{FALSE} otherwise.
}
\description{
Prepares the data contained in a \code{databook} object for upload and writes it
to the database. This includes adding the source ETL script \code{script_id} and,
when applicable, resolving \code{source_id} values to \code{participant_id} values and
\code{event_id} using the \code{id_mapping} table.
}
\details{
Special handling is applied depending on \code{databook$codebook_name}:
\itemize{
\item \code{"id_mapping"}: Adds a deterministic \code{participant_id} column via \code{.addParticipantIds()}.
\item Other tables: If the data contains a \code{source_id} column, the function
joins against the database \code{id_mapping} table to add \code{participant_id}.
The \code{source_id} column is then set to \code{NA}.
\item Other tables without \code{source_id}: if \code{participant_id} is already present
in the data it is used directly; this is logged at \code{info} level.
}

If the data is successfully uploaded to the database, \code{table_script}
is also updated with metadata for that \code{script_id}, including the MD5 hash
of the ETL script and the codebook version.
}
\examples{
\dontrun{
FAIRgroundConnect("LUCID_FAIRPOINT_TEST")
script_id <- "studies/LaCHMI/scripts/participant.R"
databook <- sourceScript(script_id = script_id)
databasePrepareData(databook, script_id = script_id)
}

}
